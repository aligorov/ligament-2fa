# Бинарник
/twofa
/twofa-*

# Секреты и локальное окружение
.env
*.local.yaml

# Секретные/локальные артефакты (audit P1: никогда не коммитить —
# lic.pem — выданная лицензия, tinytuya.json — Tuya-креды,
# snapshot.json — LAN-снимок, cmd/licgen/licgen — вендорский бинарник)
lic.pem
tinytuya.json
snapshot.json
cmd/licgen/licgen
# Пароль первого администратора (bootstrap, main.go)
admin_password.txt

# Go
/bin/
*.test
*.out
coverage.*

# macOS
.DS_Store

# superpowers SDD-скретч (брифы/отчёты/ledger/worktrees)
.superpowers/
graphify-out/

